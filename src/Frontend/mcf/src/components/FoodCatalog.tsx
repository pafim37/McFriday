import { Typography } from "@mui/material";
import React from "react";

const FoodCatalog : React.FC = (props) => {
    return <Typography variant="h3" className="header">Witaj w restauracji McFriday</Typography>
}

export default FoodCatalog;